<!-- Navbar Start -->
    <nav class="navbar navbar-expand-lg bg-white navbar-light sticky-top py-lg-0 px-lg-5 wow fadeIn" data-wow-delay="0.1s">
        <a href="{{ path('app_trick_index') }}" class="navbar-brand ms-4 ms-lg-0">
            <h1 class="text-primary m-0"><img class="me-3" src="/img/snowtricksLogo.png" ></h1>
        </a>
        <button type="button" class="navbar-toggler me-4" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarCollapse">
            <div class="navbar-nav ms-auto p-4 p-lg-0">
                <a href="{{ path('app_trick_index') }}" class="nav-item nav-link">Tricks</a>
                {% if not app.user %}
                    <a href="{{ path('app_login') }}" class="nav-item nav-link">Se Connecter</a>
                    <a href="{{ path('app_register') }}" class="nav-item nav-link">S'inscrire</a>
                {% endif %}
                {% if app.user %}
                    <a href="{{ path('app_logout') }}" class="nav-item nav-link">Sign out</a>
                {% endif %}
            </div>
        </div>
    </nav>
    <!-- Navbar End -->

    <!-- Script -->
